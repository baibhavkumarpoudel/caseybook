<%- include('../partials/boilerplate') %>
<body>
    <% for (let i=0; i < users.length; i++) { %>
            <img src=<%=users[i].img %> alt="User" />
            <p><%= users[i].aboutMe %></p>
        <% } %>
</body>
</html>